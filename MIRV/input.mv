def proc myproc () = { 1 - 2 - 3; }
def func real somefunc (int x, real y) = 
{ // blagh
	def int z = 7; 
	set z = x * 2; // blargh
 	return x + y;
} 
local proc myproc () = {
	let real x = 5.7;
	set x = 'pu""rple'; // should error once type checking is in place
}
foo = 5 * 30; bar = bar - 60.6; 7.>>
let x = .5;
x + x - x -- @x "som'e str\ing" ++ x / x "some ot\\her\\\\\ string\\" * x ** x 
f(x); // comments are allowed!
{{{{x = 3, 2; 'more " string'}}}}
int x[4] = 27;
// comment2
